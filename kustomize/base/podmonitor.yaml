apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: egress-checker
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: egress-checker
  podMetricsEndpoints:
    - port: metrics
      interval: 30s
      path: /metrics
